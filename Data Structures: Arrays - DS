#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    int number;
    cin >> N;
    int foo[N];
    for (int i = N - 1; i >= 0; i--) {
        cin >> foo[i];
    }
    for (int i = 0; i < N; i++) {
        cout << foo[i] << " ";
    }
    return 0;
}
